[pytest]
asyncio_mode = strict
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = backend/tests

# Filter warnings from third-party libraries that we cannot fix directly
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning